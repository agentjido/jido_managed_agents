# Optional provider credentials for real LLM-backed runs.
# The seeded dashboard data and automated tests do not require these.

ANTHROPIC_API_KEY=replace-me
OPENAI_API_KEY=replace-me

# Optional when the app is not reachable at http://127.0.0.1:$PORT
JIDO_MANAGED_AGENTS_MCP_BASE_URL=http://127.0.0.1:4000
