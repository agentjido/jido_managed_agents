# Real provider credentials for local managed-agent smoke runs.
# Load these into your shell before running `mix managed_agent` or `mix ma`.

ANTHROPIC_API_KEY=replace-me
OPENAI_API_KEY=replace-me

# Optional when the app or MCP endpoint is not reachable at http://127.0.0.1:$PORT
JIDO_MANAGED_AGENTS_MCP_BASE_URL=http://127.0.0.1:4000
