name: CI

on:
  pull_request:
  merge_group:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  ci:
    name: CI
    uses: agentjido/github-actions/.github/workflows/jido-ci.yml@v5
    secrets: inherit
    with:
      otp_versions: '["27", "28"]'
      elixir_versions: '["1.18", "1.19"]'
      experimental_compile_elixir_versions: '["v1.20.0-rc.6"]'
      experimental_compile_otp_versions: '["28.4.1"]'
      experimental_compile_otp_name: '28'
      validate_hex_package: false
      docs: false
      credo_command: ":"
      dialyzer_command: ":"
      test_setup_command: |
        sudo systemctl start postgresql.service
        sudo -u postgres psql -c "ALTER USER postgres PASSWORD 'postgres';"
        pg_isready -h localhost -p 5432 -U postgres
      test_command: mix test
